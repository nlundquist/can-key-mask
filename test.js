/**
 * Created by Nils Lundquist (nils@bitovi.com) on 2017-11-04.
 */

import 'steal-mocha';
import 'can-key-mask';

// TODO: add eslint
// pattern works with key-mask attribute
// pattern works with key-mask-pattern attribute
// warn thrown if no configuration passed
// warn thrown if pattern is not valid
// basic use works via can-attr & directly
// throw error if configurations are mixed, no support for multiple masks currently